# Sunset Core (Core Nest Library)

## Installation

The Sunset Nest CORE requires [Node.js](https://nodejs.org/) v18+ to run.

```sh
cd /path/your_project
```


```sh
npm i @sunset-cli/core --save
```
